# Live Mood Architect

Full-stack app with a FastAPI backend and a Vite + React frontend (frontend scaffold pending).

## Backend

From ackend/:

1. pip install -r requirements.txt
2. uvicorn main:app --reload

Health check: GET /health returns {\"status\": \"ok\"}.
